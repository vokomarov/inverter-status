# Inverter Status

ESP32 project with display to render HomeAssistant entities

## Setup

- Create personal access token in Home Assistant
- Copy `secrets.h.example` to `secrets.h`
- Fill WiFi settings, Home Assistant host and port, personal access token
